import { createMuiTheme } from "@material-ui/core";

const dark = createMuiTheme({
	palette: {
		primary: { main: '#ffffff' },
		secondary: { main: '#ffffff' },
		type: 'dark'
	},
});

export default dark;
